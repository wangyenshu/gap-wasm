assign(domain_size, 2).
assign(increment, 1).

assign(max_megs, -1).
assign(max_models, -1).
assign(max_seconds, -1).

assign(selection_order, 2).
assign(selection_measure, 4).

set(negprop).
set(neg_assign).
set(neg_assign_near).

set(neg_elim).

set(print_models).
set(lnh).

formulas(assumptions).
  all x all y all z    (x * y) * z != x * (y * z).
end_of_list.
